import {Box} from "@mui/material";

const StackAndTools = () => {
    return (
        <Box component={'section'} sx={{
            width: "100%",
            minHeight: "100vh",
            background: "#434242"
        }}>
            StackAndTools
        </Box>
    );
};

export default StackAndTools;