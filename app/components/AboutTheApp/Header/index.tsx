import {Box} from "@mui/material";

const Header = () => {
    return (
        <Box component={'header'} sx={{
            width: "100%",
            minHeight: "100vh",
        }}>
            Header
        </Box>
    );
};

export default Header;